"use client";import Image from"next/image";import Link from"next/link";import{useStore}from"@/components/store-provider";import{formatTL}from"@/data/catalog";export default function Cart(){const{cart,removeCart,quantity}=useStore();const total=cart.reduce((n,x)=>n+x.product.price*x.quantity,0);return <div className="container page"><div className="title"><h1>Sepetim</h1><p>Ürünlerini kontrol et ve alışverişini tamamla.</p></div>{!cart.length?<div className="empty"><i className="fa-solid fa-cart-shopping"/><h2>Sepetin şu an boş</h2><p>Binlerce ürün arasından ihtiyacın olanları keşfet.</p><Link href="/">Alışverişe Başla</Link></div>:<div className="cart-layout"><section className="cart-lines">{cart.map(x=><article key={x.product.id}><input type="checkbox" defaultChecked/><Image src={x.product.image} width={115} height={115} alt={x.product.name}/><div><h3>{x.product.name}</h3><p>Kargo Bedava · Hızlı Teslimat</p><div className="quantity"><button onClick={()=>quantity(x.product.id,-1)}>−</button><span>{x.quantity}</span><button onClick={()=>quantity(x.product.id,1)}>+</button></div></div><strong>{formatTL(x.product.price*x.quantity)}</strong><button className="trash" onClick={()=>removeCart(x.product.id)}><i className="fa-solid fa-trash"/></button></article>)}</section><aside className="summary"><h2>Sipariş Özeti</h2><p>Ürünler <strong>{formatTL(total)}</strong></p><p>Kargo <strong>Ücretsiz</strong></p><hr/><p className="total">Toplam <strong>{formatTL(total)}</strong></p><Link href="/odeme">Sepeti Onayla</Link><small><i className="fa-solid fa-shield-halved"/> Güvenli ödeme altyapısı</small></aside></div>}</div>}
