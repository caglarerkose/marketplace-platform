import Image from "next/image";
import { PageFrame } from "@/components/layout/page-frame";
import { products } from "@/data/home";
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const product=products.find(item=>item.id===slug)??products[0];const price=new Intl.NumberFormat("tr-TR",{style:"currency",currency:"TRY"}).format(product.price/100);return <PageFrame title=""><div className="detail-layout"><section className="detail-gallery"><Image src={product.image} alt={product.name} fill sizes="(max-width:768px) 100vw, 40vw"/></section><section className="detail-info"><small>Elektronik</small><h1>{product.name}</h1><p className="detail-rating">★ {product.rating} ({product.reviews} değerlendirme)</p><strong className="detail-price">{price}</strong><div className="campaign-box">Sepette avantajlı fiyat</div><div className="detail-actions"><span className="mobile-action-price"><small>Ödenecek</small><strong>{price}</strong></span><button><i className="fa-solid fa-bolt"/> Hemen Al</button><button><i className="fa-solid fa-cart-shopping"/> Sepete Ekle</button></div></section><aside className="panel"><h2>Satıcı Bilgisi</h2><strong>BişeyEksik Mağazası</strong><p>9.6 satıcı puanı</p></aside></div></PageFrame>}
