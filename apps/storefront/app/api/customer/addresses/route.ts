import { NextResponse } from "next/server";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import { addressSchema } from "@/lib/customer-address";

export async function GET(){const client=await createSupabaseServerClient(),{data:{user}}=await client.auth.getUser();if(!user)return NextResponse.json({error:"Oturum gerekli."},{status:401});const{data,error}=await client.from("customer_addresses").select("id,title,recipient_name,phone,city,district,neighborhood,address_line,postal_code,is_default,created_at").eq("user_id",user.id).order("is_default",{ascending:false}).order("created_at");if(error)return NextResponse.json({error:"Adresler alınamadı."},{status:500});return NextResponse.json({addresses:data||[]})}
export async function POST(request:Request){const client=await createSupabaseServerClient(),{data:{user}}=await client.auth.getUser();if(!user)return NextResponse.json({error:"Oturum gerekli."},{status:401});const parsed=addressSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Adres bilgilerini kontrol edin."},{status:400});const{count}=await client.from("customer_addresses").select("id",{count:"exact",head:true}).eq("user_id",user.id);const{data,error}=await client.from("customer_addresses").insert({user_id:user.id,title:parsed.data.title,recipient_name:parsed.data.recipientName,phone:parsed.data.phone,city:parsed.data.city,district:parsed.data.district,neighborhood:parsed.data.neighborhood||null,address_line:parsed.data.addressLine,postal_code:parsed.data.postalCode||null,is_default:(count||0)===0}).select("id").single();if(error)return NextResponse.json({error:"Adres kaydedilemedi."},{status:500});return NextResponse.json({ok:true,address:data},{status:201})}
