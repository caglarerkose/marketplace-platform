import{ProductBrowser}from"@/components/product-browser";import{getActiveProducts}from"@/lib/catalog-products";export default async function Category({params}:{params:Promise<{slug:string}>}){const{slug}=await params,products=await getActiveProducts(),items=products.filter(product=>product.categorySlug===slug),title=items[0]?.category||decodeURIComponent(slug).split("-").map(part=>part[0]?.toUpperCase()+part.slice(1)).join(" ");return <ProductBrowser items={items} title={title} description="Seçili kategorideki ürünleri keşfet."/>}
