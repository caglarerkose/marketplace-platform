"use client";import{useParams}from"next/navigation";import{ProductBrowser}from"@/components/product-browser";import{products}from"@/data/catalog";export default function Category(){const{slug}=useParams<{slug:string}>(),title=decodeURIComponent(slug).split("-").map(x=>x[0]?.toUpperCase()+x.slice(1)).join(" "),items=products.filter(p=>p.category.toLocaleLowerCase("tr-TR")===title.toLocaleLowerCase("tr-TR"));return <ProductBrowser items={items.length?items:products} title={title} description="Seçili kategorideki ürünleri keşfet."/>}
