"use client";import Image from"next/image";import Link from"next/link";import type{Product}from"@/data/catalog";import{formatTL}from"@/data/catalog";import{useStore}from"./store-provider";export function ProductCard({product}:{product:Product}){const{favorites,toggleFavorite}=useStore();return <article className={`product-card mode-${product.priceMode}`}><button className={favorites.includes(product.id)?"fav active":"fav"} onClick={()=>toggleFavorite(product.id)}><i className="fa-solid fa-heart"/></button><Link href={`/urun/${product.id}`}><Image className="product-img" src={product.image} width={260} height={260} alt={product.name}/><span className="product-badge"><i className={`fa-solid ${product.priceMode==="percent"?"fa-arrow-down":product.priceMode==="cart-discount"?"fa-basket-shopping":"fa-bolt"}`}/>{product.badge}</span><div className="product-info"><h3>{product.name}</h3><p className="shipping">Kargo Bedava</p><div className="rating">★ ★ ★ ★ ★ <span>({product.reviewCount})</span></div><div className="price">{product.price!==product.originalPrice&&<del>{formatTL(product.originalPrice)}</del>}<strong>{formatTL(product.price)}</strong></div></div></Link></article>}
