"use client";import Image from"next/image";import Link from"next/link";import{useStore}from"./store-provider";import{formatTL}from"@/data/catalog";export function StoreToast(){const{toast,lastAdded,closeAdded}=useStore();return <><div className={`store-toast ${toast?"show":""}`}><i className="fa-solid fa-circle-check"/>{toast}</div>{lastAdded&&<div className="cart-popup-backdrop" onClick={closeAdded}><section className="cart-popup" onClick={e=>e.stopPropagation()}><button className="popup-close" onClick={closeAdded}>×</button><i className="fa-solid fa-circle-check popup-check"/><h2>Ürün sepete eklendi</h2><div className="popup-product"><Image src={lastAdded.image} width={78} height={78} alt={lastAdded.name}/><div><strong>{lastAdded.name}</strong><b>{formatTL(lastAdded.price)}</b></div></div><div className="popup-actions"><button onClick={closeAdded}>Alışverişe Devam Et</button><Link href="/sepet" onClick={closeAdded}>Sepete Git</Link></div></section></div>}</>}
