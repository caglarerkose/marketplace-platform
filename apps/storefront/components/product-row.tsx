"use client";import Link from"next/link";import{useRef}from"react";import type{Product}from"@/data/catalog";import{ProductCard}from"./product-card";export function ProductRow({title,products,tone="orange"}:{title:string;products:Product[];tone?:string}){const row=useRef<HTMLDivElement>(null),move=(d:number)=>row.current?.scrollBy({left:d*row.current.clientWidth*.82,behavior:"smooth"});return <section className={`vitrin ${tone}`}><header><div><small>SANA ÖZEL</small><h2>{title}</h2></div><Link href="/kategori/elektronik">Tümünü Gör →</Link></header><div className="vitrin-body"><button className="vitrin-arrow left" onClick={()=>move(-1)} aria-label="Önceki ürünler">‹</button><div className="product-row" ref={row}>{products.map(p=><ProductCard product={p} key={p.id}/>)}</div><button className="vitrin-arrow right" onClick={()=>move(1)} aria-label="Sonraki ürünler">›</button></div></section>}
