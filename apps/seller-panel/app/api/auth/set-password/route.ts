import{NextResponse}from"next/server";import{z}from"zod";import{createAuthServerClient}from"@/lib/supabase/server";const schema=z.object({password:z.string().min(8)});export async function POST(request:Request){const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Şifre en az 8 karakter olmalıdır."},{status:400});const auth=await createAuthServerClient(),{data:{user}}=await auth.auth.getUser();if(!user)return NextResponse.json({error:"Aktivasyon oturumu bulunamadı."},{status:401});const{error}=await auth.auth.updateUser({password:parsed.data.password});if(error)return NextResponse.json({error:"Şifre kaydedilemedi. Bağlantıyı yeniden talep edin."},{status:400});return NextResponse.json({ok:true})}
