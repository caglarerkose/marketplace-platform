"use client";
import {FormEvent,useEffect,useState} from "react";import {useRouter} from "next/navigation";import {AuthHeader} from "@/components/auth-header";import {createAuthBrowserClient} from "@/lib/supabase/client";
export default function SetPassword(){const router=useRouter(),[ready,setReady]=useState(false),[error,setError]=useState(""),[saving,setSaving]=useState(false);useEffect(()=>{const prepare=async()=>{const auth=createAuthBrowserClient(),hash=new URLSearchParams(window.location.hash.slice(1)),access=hash.get("access_token"),refresh=hash.get("refresh_token");if(access&&refresh){const {error:sessionError}=await auth.auth.setSession({access_token:access,refresh_token:refresh});if(sessionError){setError("Aktivasyon bağlantısı geçersiz veya süresi dolmuş.");return}}const {data}=await auth.auth.getUser();if(!data.user){setError("Aktivasyon bağlantısı geçersiz veya süresi dolmuş.");return}setReady(true)};void prepare()},[]);const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();const form=new FormData(event.currentTarget),password=String(form.get("password")),confirm=String(form.get("confirm"));if(password!==confirm)return setError("Şifreler eşleşmiyor.");setSaving(true);const auth=createAuthBrowserClient(),{error:updateError}=await auth.auth.updateUser({password});if(updateError){setError("Şifre kaydedilemedi. Bağlantıyı yeniden talep edin.");setSaving(false);return}router.replace("/application-status");router.refresh()};return <div className="auth-page"><AuthHeader showSellerLogin={false}/><main className="auth-main password-main"><form className="login-card-ref" onSubmit={submit}><h2>Şifrenizi Belirleyin</h2><p>Satıcı hesabınız için yalnızca sizin bildiğiniz güvenli bir şifre oluşturun.</p>{error&&<div className="apply-error"><i className="fa-solid fa-circle-exclamation"/>{error}</div>}{ready&&<><label className="login-input"><i className="fa-solid fa-lock"/><input name="password" type="password" required minLength={8} maxLength={128} autoComplete="new-password" placeholder="Yeni şifre"/></label><label className="login-input"><i className="fa-solid fa-lock"/><input name="confirm" type="password" required minLength={8} maxLength={128} autoComplete="new-password" placeholder="Yeni şifre tekrar"/></label><button className="auth-primary-btn" disabled={saving}>{saving?"KAYDEDİLİYOR...":"ŞİFREYİ KAYDET"}</button></>}</form></main></div>}
